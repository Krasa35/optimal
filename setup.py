from setuptools import setup, find_packages

setup(
    name="optimal",            # Name of your package
    version="0.0.1",              # Initial version
    packages=find_packages(),     # Automatically finds folders with __init__.py
)